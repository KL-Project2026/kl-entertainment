providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npx -y pnpm@10.33.2 install --frozen-lockfile"]

[phases.build]
cmds = ["npx -y pnpm@10.33.2 --filter @workspace/api-server build"]

[start]
cmd = "node artifacts/api-server/dist/index.cjs"
